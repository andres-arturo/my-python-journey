#collection = single "variable" used to store multiple values.
#   List = [] ordered and changeable. Duplicates are OK.
#   Set = {} unordered and immutable, but add/remove are OK. NO duplicates.
#   Tuple = () ordered and unchangeable. Duplicates are OK. FASTER

fruits = ["apple", "orange", "banana", "coconut"]
word = "magic"

print(dir(word))
print(help(str))
#print(dir(fruits)) #This will show all the attributes we can use for a list, set, or tuple.
#print(help(fruits)) #will print the description of each of these attributes available.
